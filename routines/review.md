# review

## When
"how was my week", "last week", "review".

## Read
Week's Days, Goals.

## Steps
1. Monday to Sunday. "how was my week"/"review" this week, "last week" the previous one. Eligible dates: this week Monday to today, last week all seven.
2. Counted: eligible `closed`/`auto-closed` Days. Open: the week's `open` Day, never counted, one line with its date. Missing: eligible dates with no Day node; state them, never guess.
3. Average per day = seven totals ÷ counted, rounded by `routines/log.md` step 4.
4. Days on target: verdicts reading `on target`. Most common miss: `<macro> low|high`, day count; ties: kcal>protein>fat>carbs, low>high.
5. None counted: `Average: n/a`, `On target: 0 of 0`, `Most common miss: none`.

## Write
Nothing: no commit.

## Reply
Under ten lines:
`Days: <n> of <eligible> closed, <m> auto-closed, missing <dates|none>`
`Average: <kcal> kcal · <P> P · <F> F · <C> C · <n> fiber · <n> sugar · <n> salt` (counted Day estimated: `~` before every number)
`Target: <kcal> kcal · <P> P · <F> F · <C> C`
`On target: <n> of <days>`
`Most common miss: <macro> <low|high>, <n> days[; <macro> <low|high>, <n> days]` or `none`
`<date> is open and not counted.`
